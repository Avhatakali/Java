//import java.applet.*; // this isn't allowed, if the is package declaration
package academy.learnProgramming; // imports cannot be before package.
import java.lang.*;
// package academy; //unexpectected token, it shouldn't be here
public class OrderingElement {
    public static void main(String[] args) {

        /*
        void myMethod() {   // you cannot declare a method inside a method main

        }   */
    }

    //but yuh can have as many method as you can inside the class

    //PIC (package, import, class)
}
